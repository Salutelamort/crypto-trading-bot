# Инфраструктура — чистый Python (принцип из треда: LLM не считает математику)
requests==2.34.2
pandas==2.2.3
numpy==2.3.5
PyYAML==6.0.3

# Для макро-фильтра (парсинг таблиц потоков в ETF с farside.co.uk)
lxml==6.1.1
html5lib==1.1

# Веб-дашборд (удобный интерфейс)
Flask==3.1.2

# CI/локальная проверка качества
ruff==0.16.5

# Опционально: уровень управления (Claude как супервизор).
# Бот полностью работает БЕЗ этого пакета — тогда супервизор использует
# детерминированные правила. anthropic нужен только если хочешь, чтобы
# управленческие решения принимал Claude.
