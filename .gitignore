# Книги — НЕ заливать в GitHub (огромные + авторские права!)
library/

# Временное / системное
__pycache__/
*.pyc
scratchpad/
*.log

# Секреты (ключи API и т.п.) — никогда в репозиторий
secrets.local.json
*.key

# Экспорты чатов
messages*.html

# ВАЖНО: data/bot.db НЕ игнорируем — это и есть "память" обучения,
# она должна сохраняться в репозитории между запусками робота.
