name: daily-learn

# Trading and research moved to the single Railway volume owner.
# This workflow deliberately has no schedule and cannot mutate account state.
on:
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  moved:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Paper trading and research run on Railway. See docs/railway.md."
